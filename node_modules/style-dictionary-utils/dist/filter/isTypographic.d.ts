import StyleDictionary from 'style-dictionary';
/**
 * @name isTypographic
 * @type filter
 * @description only returns tokens of type `typography`, `fontFamily`
 */
export declare const isTypographic: (token: StyleDictionary.TransformedToken) => boolean;

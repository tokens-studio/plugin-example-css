import StyleDictionary from 'style-dictionary';
/**
 * @name isStrokeStyle
 * @type filter
 * @description only returns tokens of type `strokeStyle`
 */
export declare const isStrokeStyle: (token: StyleDictionary.TransformedToken) => boolean;

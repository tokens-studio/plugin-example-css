import StyleDictionary from 'style-dictionary';
/**
 * @name isTransition
 * @type filter
 * @description only returns tokens of type `transition`
 */
export declare const isTransition: (token: StyleDictionary.TransformedToken) => boolean;

/**
 * @name w3cTokenJson5Parser
 * @type parser
 * @description parses json5 and replace `$value` with `value` and `$description`
 * with `comment` to make it work with style dictionary
 */
export declare const w3cTokenJson5Parser: {
    pattern: RegExp;
    parse: ({ contents }: {
        contents: string;
    }) => any;
};

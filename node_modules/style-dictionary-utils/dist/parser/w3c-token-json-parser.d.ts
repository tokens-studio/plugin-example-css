/**
 * @name w3cTokenJsonParser
 * @type parser
 * @description parses json and replace `$value` with `value` and `$description`
 * with `comment` to make it work with style dictionary
 */
export declare const w3cTokenJsonParser: {
    pattern: RegExp;
    parse: ({ contents }: {
        contents: string;
    }) => any;
};
